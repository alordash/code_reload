fn main() {
    code_reload_build::library();

    // let out_dir = std::env::var("OUT_DIR").unwrap();
    // p!("OUT_DIR: '{:?}'", out_dir);
    // 
    // for (key, value) in std::env::vars() {
    //     p!("'{}' = {}", key, value);
    // }
}
